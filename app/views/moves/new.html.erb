<%= simple_nested_form_for(@move) do |f| %>
  
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :max_number %>
    <%= f.input :mtype %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>	
  </div>

<% end %>

<%= link_to "Back to Moves", moves_path %>
